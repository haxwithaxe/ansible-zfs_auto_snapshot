PATH="/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin"

*/{{ zfs_auto_snapshot_frequent_interval }} * * * * root which zfs-auto-snapshot > /dev/null || exit 0 ; zfs-auto-snapshot --quiet --syslog --label=frequent --keep={{ zfs_auto_snapshot_frequent_keep }} //
